// Rutas para apps móviles simuladas (TinderRP, UberRP, DeliverooRP)
const express = require('express');
const router = express.Router();

// TODO: Endpoints para cada app RP

module.exports = router;
